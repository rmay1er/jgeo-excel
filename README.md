# 🗺️ excel-cords-to-geojson

CLI приложение для преобразования координат из Excel файлов в формат GeoJSON.

![Go](https://img.shields.io/badge/Go-1.25.1-blue)
![License](https://img.shields.io/badge/License-MIT-green)
![Architecture](https://img.shields.io/badge/Architecture-Processor%20Pattern-brightgreen)

## 📖 Описание

**excel-cords-to-geojson** — это инструмент командной строки для преобразования географических координат из Excel таблиц в формат GeoJSON.

### Основные возможности

- 📊 **Чтение Excel файлов** — произвольные столбцы и листы
- 🗺️ **GeoJSON интеграция** — добавление точек в существующий файл
- ⚙️ **Гибкая конфигурация** — настройка через YAML файл
- 🎨 **Кастомизация** — настройка цвета маркеров
- ✅ **Валидация данных** — проверка корректности координат
- 📍 **Поддержка множества листов**
- 🔌 **Модульная архитектура** — легко добавлять новые Reader/Writer

## 🚀 Быстрый старт

### Установка зависимостей

```bash
go mod tidy
```

### Сборка приложения

```bash
go build -o excel-cords-to-geojson main.go
```

### Использование

1. Скопируйте пример конфигурации:
```bash
cp config.example.yaml config.yaml
```

2. Отредактируйте `config.yaml`

3. Запустите приложение:
```bash
./excel-cords-to-geojson convert --config config.yaml
```

## 📋 Конфигурационный файл

### Пример config.yaml

```yaml
excel:
  file: "data.xlsx"              # Путь к Excel файлу
  sheet: "Sheet1"                # Название листа
  columns:
    name: "A"                    # Столбец с названиями
    description: "B"             # Столбец с описаниями
    coordinates: "C"             # Столбец с координатами
  start_row: 2                   # Начало данных

geojson:
  input: "base.geojson"          # Базовый GeoJSON файл
  output: "result.geojson"       # Результирующий файл

appearance:
  marker_color: "#FF0000"        # Цвет маркера
```

### Параметры

#### Excel

| Параметр | Описание | Тип | Обязательный | По умолчанию |
|----------|---------|-----|-------------|--------------|
| `file` | Путь к Excel файлу | string | ✅ Да | - |
| `sheet` | Название листа | string | ❌ Нет | "Sheet1" |
| `columns.name` | Столбец с названием | string | ✅ Да | - |
| `columns.description` | Столбец с описанием | string | ✅ Да | - |
| `columns.coordinates` | Столбец с координатами | string | ✅ Да | - |
| `start_row` | Строка начала данных | int | ❌ Нет | 2 |

#### GeoJSON

| Параметр | Описание | Тип | Обязательный |
|----------|---------|-----|-------------|
| `input` | Входной GeoJSON | string | ✅ Да |
| `output` | Выходной GeoJSON | string | ✅ Да |

#### Appearance

| Параметр | Описание | Тип | По умолчанию |
|----------|---------|-----|--------------|
| `marker_color` | Цвет маркера (hex) | string | "#FF0000" |

## 📊 Формат данных

### Excel файл

Таблица должна содержать следующие колонки:

| Столбец | Содержимое | Пример |
|---------|-----------|--------|
| A | Названия точек | "Москва" |
| B | Описания | "Столица России" |
| C | Координаты | "55.75 37.62" |

**Формат координат:** `широта долгота` (через пробел) или `широта,долгота`

### GeoJSON файл

#### Входной файл (base.geojson)

Должен быть корректным GeoJSON FeatureCollection:

```json
{
  "type": "FeatureCollection",
  "features": []
}
```

#### Выходной файл (result.geojson)

Новые точки добавляются в формате:

```json
{
  "type": "Feature",
  "geometry": {
    "type": "Point",
    "coordinates": [37.62, 55.75]
  },
  "properties": {
    "iconCaption": "Москва",
    "description": "Столица России",
    "marker-color": "#FF0000"
  }
}
```

## 💻 Примеры использования

### Пример 1: Базовое использование

```bash
excel-cords-to-geojson convert -c config.yaml
```

Вывод:
```
📂 Загружаю конфигурацию из: config.yaml
✅ Конфигурация загружена успешно
  📊 Excel файл: data.xlsx (лист: Sheet1)
  📍 Столбцы: название=A, описание=B, координаты=C
  🗺️  GeoJSON: base.geojson → result.geojson

🔧 Инициализирую приложение...

🔄 Начинаю преобразование координат...
📖 Читаю данные из источника...
✅ Прочитано 5 координат
✍️  Записываю данные в целевой формат...
✅ Данные записаны успешно
💾 Сохраняю результат в: result.geojson

✅ Успешно! Результат сохранен в: result.geojson
```

### Пример 2: Удаление всех точек из GeoJSON

```bash
excel-cords-to-geojson removepoints --file result.geojson
```

### Паттерны проектирования

1. **Processor Pattern** — основной поток: Read → Process → Write
2. **Interface Segregation** — Reader и Writer как отдельные интерфейсы
3. **Dependency Injection** — зависимости передаются в конструктор
4. **Facade Pattern** — App упрощает взаимодействие с компонентами

## 📝 Лицензия

MIT License - смотрите файл LICENSE для деталей.

---

**Версия:** 1.1.101  
**Последнее обновление:** 15.12.2025
